version: '3'
services:
  openvpn:
    build:
      context: .
      dockerfile: ./docker/openvpn/Dockeerfile
    image: skings-openvpn:latest
    container_name: skings-openvpn
    tty: true
    port:
      - 4040:4040
